<nav class="navbar navbar-dark bg-dark">
  <a class="navbar-brand" href="{{ path_for('home') }}"><img src="{{ base_url() }}/img/atom.svg" width="100" height="100" alt=""></a>
  <ul class="navbar nav mr-auto">
    <li class="nav-item"><a class="nav-link" href="{{ path_for('cart.index') }}" style="color:white"><img src="{{ base_url() }}/img/shopping-cart.svg" width="35">Cart ({{ basket.itemCount }})</a></li>
  </ul>
  {% if auth.check %}
  <h4 style="color:white">{{ auth.user.name }}</h4>
  <h4 style="color:#373C42">_</h4>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse flex-grow-1 text-right" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li>
          <a class="nav-link" href="{{ path_for('library.index') }}" style="color:white"><img src="{{ base_url() }}/img/playlist.svg" width="42" height="42">Your Library</a>
        </li>
        <li>
          <!-- <a class="nav-link" href="#" style="color:white">Change password</a> -->
        </li>
        <li>
          <a class="nav-link" href="{{ path_for('auth.signout') }}" style="color:white">Sign Out</a>
        </li>
      </ul>
    </div>
    {% else %}
    <ul class="navbar nav ml-auto">
      <li><a class="nav-link" href="{{ path_for('auth.signin') }}"  style="color:white">Sign in</a></li>
      <li><a class="nav-link" href="{{ path_for('auth.signup') }}" style="color:white">Sign up</a></li>
    </ul>
    {% endif %}
</nav>
